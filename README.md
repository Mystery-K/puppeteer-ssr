# ssr-puppeteer

## Install
	npm install ssr-puppeteer

## Usage
	const generateHTML = require("../ssr-puppeteer");
	/**
	* 
	* @param {String} _url 
	* @param {String} name 
	* @param {String} dist 
 	*/
	generateHTML("http://mp.sina.com.cn");
